# Platform checklist — generic

No platform profile is configured for this repository in `org-claude-config.yml`.
Apply `_org.md` and lean on the repository's `CLAUDE.md` for local conventions.

If this repository is a real product surface, add it to `org-claude-config.yml`
with the right `platform` so it gets a proper checklist.
